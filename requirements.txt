# Floor pins only: Streamlit Cloud runs Python 3.14 — exact old pins forced
# source builds (no cp314 wheels) which hang/fail. Floors let pip resolve
# the latest prebuilt wheels while keeping known-good minimums.
streamlit>=1.41
pandas>=2.3
numpy>=2.1
pyarrow>=18
